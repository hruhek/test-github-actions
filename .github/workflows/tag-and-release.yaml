name: Tag and Release

on:
  pull_request:
    types:
      - closed

jobs:
  release:
    runs-on: ubuntu-latest
    if: startsWith(github.event.pull_request.title, 'Release:') && github.event.pull_request.merged
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: ./.github/actions/setup-env
      - name: Install dependencies
        run: poetry install
#      - name: Setup Git
#        run: |
#          git config user.name "hruhek"
#          git config user.email "hrvoje.ruhek@wearenotch.com"
      - name: Get tag
        id: get_tag
        run: |
#          git branch --show-current
#          git pull
          echo "VERSION=v$(poetry version --short)" >> $GITHUB_ENV
      - name: Tag the commit
        run: |
          new_version=${{ env.VERSION }}
          git tag -a "$new_version" -m "Version $new_version"
          git push --follow-tags